---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: woodpecker
  annotations:
    external-dns.alpha.kubernetes.io/remote-enabled: "true"
spec:
  parentRefs:
    - name: envoy-gateway
      namespace: network
      sectionName: https
  hostnames:
    - woodpecker.lab.egenintres.se
  rules:
    - backendRefs:
        - name: woodpecker-server
          port: 80
