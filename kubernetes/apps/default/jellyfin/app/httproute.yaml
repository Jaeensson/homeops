---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin
spec:
  parentRefs:
    - name: envoy-gateway
      namespace: network
      sectionName: https
  hostnames:
    - jf.lab.egenintres.se
  rules:
    - backendRefs:
        - name: jellyfin
          port: 8096
